// import type { Entities } from '../entities';
import type { NoteClip } from './noteClip';

// ready to add more later
export type Clip = NoteClip;

// export const addClip = (entities: Entities, clipConfig: NoteClip) => {
//   return {
//     ...entities,
//     clips: entities.clips,
//   };
// };
